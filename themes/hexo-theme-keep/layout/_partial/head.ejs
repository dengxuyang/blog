<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <meta name="keywords" content="<%= config.keywords || '邓旭阳的技术博客,coderun,技术分享,微服务' %>">
    <meta name="description" content="<%= config.description || '技术分享,邓旭阳的技术博客' %>">
    <meta name="author" content="<%= theme.base_info.author || config.author || 'Keep Team' %>">
    <%
    let title = page.title;
    if (is_archive()) {
        title = __('archive');
        if (is_month()) {
            title += ': ' + page.year + '/' + page.month;
        } else if (is_year()) {
            title += ': ' + page.year;
        }
    } else if (is_category()) {
        title = __('category') + ': ' + page.category;
    } else if (is_tag()) {
        title = __('tag') + ': ' + page.tag;
    } else {
        title = __(page.title);
    }
    %>
    <title>
        <% if (title) { %>
            <%= title %> |
        <% } %>
        <%= theme.base_info.title || config.title || 'Keep Theme' %>
    </title>
    <%- favicon_tag(theme.style.favicon) %>
    <%- css('css/style') %>
    <%- __css('css/font-awesome.min.css') %>
    <%- export_config() %>
</head>

